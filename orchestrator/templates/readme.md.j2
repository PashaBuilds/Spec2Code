# {{ spec.project.name }} - generated drivers

Generated by **Spec2Code** for platform `{{ spec.project.platform }}`,
core `{{ spec.project.target_core }}`, runtime `{{ spec.project.runtime }}`.
All files use **CRLF** line endings and follow `{{ spec.coding_standard_ref }}`.

## Files

| File | Purpose |
|------|---------|
{% for unit in units %}| `drivers/{{ unit.module }}.h` / `.c` | {{ unit.part }} - {{ unit.summary }} |
{% endfor %}{% for unit in units %}{% if unit.test %}| `tests/{{ unit.module }}_test.h` / `.c` | {{ unit.part }} self-test ({{ unit.test.runtime }}) |
{% endif %}{% endfor %}| `tests/spec2code_mock_bus.h` / `.c` | Boardless transfer recorder for mock/self-test review |
| `tests/{{ spec.project.name }}_mock_plan.h` / `.c` | Expected init-transfer plan for boardless review |

## How to integrate (drop-in)

1. Copy `drivers/*.c` and `drivers/*.h` into your Vitis application sources.
2. Copy `tests/*_test.h` and `tests/*_test.c` if you want the self-tests.
3. Include the header(s) and call the init + operations, e.g.:

```c
{% for unit in units %}{% if unit.transport != 'i2c_mux' %}#include "{{ unit.module }}.h"
{% endif %}{% endfor %}

/* ... after your BSP is up ... */
{% for unit in units %}{% if unit.transport != 'i2c_mux' %}/* {{ unit.part }}: */
{% for n in unit.public_names %}{{ n }}(...);
{% endfor %}{% endif %}{% endfor %}```

> The drivers initialize their own controller instance via `XIicPs_LookupConfig` /
> `XSpiPs_LookupConfig` using the `xparameters.h` device IDs. Mux-attached devices select
> their channel automatically before each access.
