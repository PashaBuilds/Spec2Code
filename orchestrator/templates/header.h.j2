{% import "_macros.j2" as m -%}
/**
 * @file {{ module }}.h
 * @brief {{ summary }}
 *
 * Drop-in driver for the {{ part }}, generated by Spec2Code.
 * Coding standard: {{ ruleset_ref }}. Do not edit by hand.
 */
#ifndef {{ guard }}
#define {{ guard }}

{% for inc in header_includes %}#include "{{ inc }}"
{% endfor %}
/* --- configuration / register map --- */
{% for name, value, comment in defines %}#define {{ name }} {{ value }}{{ ("  /* " + comment + " */") if comment else "" }}
{% endfor %}

/* --- public API --- */
{% for func in public_funcs %}
{% if include_doxygen %}{{ m.doxygen(func) }}{% endif %}{{ func.signature }};
{% endfor %}

#endif /* {{ guard }} */
